server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Allow larger payloads for media uploads routed through the frontend (if proxied later)
    client_max_body_size 10m;
}
