# ===================================
# CONFIGURATION SONARQUBE
# ===================================
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins
# cp .env.example .env

# === Base de données PostgreSQL (pour SonarQube uniquement) ===
SONAR_DB_USER=sonar
SONAR_DB_PASSWORD=sonar123
SONAR_DB_NAME=sonarqube

# === Configuration SonarQube ===
SONAR_PORT=9000

# === Credentials SonarQube ===
# Par défaut: admin / admin
# IMPORTANT: Changez le mot de passe lors de la première connexion !

# === Configuration pour l'analyse de code (optionnel) ===
# Token d'authentification pour les analyses automatiques
# Générez-le depuis l'interface web: My Account > Security > Generate Token
# SONAR_TOKEN=your_sonarqube_token_here

# === Configuration avancée (optionnel) ===
# SONAR_WEB_JAVAADDITIONALOPTS=-Xmx512m
# SONAR_CE_JAVAADDITIONALOPTS=-Xmx512m
# SONAR_SEARCH_JAVAADDITIONALOPTS=-Xms512m -Xmx512m